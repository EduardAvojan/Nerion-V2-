# Nerion Policy Guide

## paths
Control where writes are allowed or denied (allow_paths / deny_paths).

## actions
Control which action kinds are allowed (allow_actions / deny_actions).

## limits
Size limits for file writes (max_file_bytes, max_total_bytes / max_diff_bytes).

## secrets
Secret/PII scanning behavior and how to remediate.

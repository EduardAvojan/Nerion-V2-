# Add Function Template

This task requires the agent to add a new function to an existing Python module. The agent must ensure the new function is correct and that it does not break the existing code.

**Parameters:**
* `function_name`: The name of the function to be added (e.g., `add_numbers`).

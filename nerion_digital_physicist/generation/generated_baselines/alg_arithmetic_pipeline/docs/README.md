# Arithmetic Pipeline Task

Implement the function `process_numbers` that applies a deterministic series of arithmetic operations to an input list. The default configuration chains operations in this order: add, multiply, clamp.
